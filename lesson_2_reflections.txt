Q-what Happen when you initialize the repository? why do you need to do it?
Ans - It creates a .git folder in whch it saves all the meta data to track all files it need to.


Q-How is the staging area different from the working directory and the repository? What value do you think it offers?
Ans - Staging area is a in betwwen stage where you put files which you wants to commit. It offer soem files names which are supposed to be commit


Q- How can you use the staging area to make sure you have one commit per logical change?
Ans- out of all files, first add those files into staging area to which ou want to commit in a logical entity.



Q-What are some situations when branches would be helpful in keeping your history organized? How would branches help?
Ans- Branches will be useful for parrelel work in a project.


Q-What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
Ans- Merging two branches make master branch on the new commit, and new commit have head as the both branches we just merged. Now from the new commit we can go to both branches so we don't need to have the branch we just merged with master.





Q- How do the diagrams help you visualize the branch structure?
Ans- To check if you are on a detached state or can you have a path from the brach to whereever you want to go etc etc.


Q-What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
Ans- Automatic merges may make code bug full.

